'use client';

import { ContactForm } from '@/components/ContactForm';

/** CTA de contacto en home y páginas internas — incluye el formulario premium */
export function ContactCTA() {
  return <ContactForm id="hablemos" />;
}
